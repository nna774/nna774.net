test:
  override:
    - echo no test
deployment:
  production:
    branch: master
    commands:
      - sed -i -e "s/deploy.user          = 'nona'/deploy.user = 'circleci'/" config.rb
      - rake deploy
