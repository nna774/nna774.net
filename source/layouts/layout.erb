<!doctype html>
<html lang="ja" prefix="og: http://ogp.me/ns#">
  <head>
    <meta charset="utf-8">
    <title><%= current_page.data.title || "いっと☆わーくす！" %></title>
    <link rel="shortcut icon" href="/favicon.ico" type="image/vnd.microsoft.icon">
    <link rel="icon" href="/favicon.ico" type="image/vnd.microsoft.icon">
    <link rel="canonical" href="https://nna774.net<%= current_page.url %>"/>
    <%= stylesheet_link_tag "default" %>
    <% if not current_page.data.customCSS.nil? %>
      <% current_page.data.customCSS.each_with_index do |css, i| %>
            <%= stylesheet_link_tag css %>
      <% end %>
    <% end %>
    <% if not current_page.data.customJS.nil? %>
      <% current_page.data.customJS.each_with_index do |js, i| %>
            <%= javascript_include_tag js %>
      <% end %>
    <% end %>
    <% if current_page.url == "/" %>
      <meta property="og:type" content="website" />
    <% else %>
      <meta property="og:type" content="article" />
    <% end %>
    <meta name="twitter:card" content="summary">
    <meta name="twitter:site" content="@nonamea774">
    <meta name="twitter:url" content="https://nna774.net<%= current_page.url %>">
    <meta name="twitter:domain" content="nna774.net">
    <meta name="twitter:creator" content="@nonamea774">

    <meta property="og:title" content="<%= current_page.data.title || "いっと☆わーくす！" %>" />
    <meta property="og:url" content="https://nna774.net<%= current_page.url %>" />
    <meta property="og:image" content="https://nna774.net/img/1012-ogp.png" />
    <meta property="og:description" content=";; This buffer is for notes you don't want to save, and for Lisp evaluation. ;; If you want to create a file, visit that file with C-x C-f, ;; then enter the text in that file's own buffer." />
 
    <%= partial "analytics" %>
    
  </head>
  
  <body class="<%= page_classes %>">
    <header>
      <h1 id="itWorks"><a href="/">いっと☆わーくす！</a></h1>
      <p id="scratch">
        ;; This buffer is for notes you don't want to save, and for Lisp evaluation.<br />
        ;; If you want to create a file, visit that file with C-x C-f,<br />
        ;; then enter the text in that file's own buffer.
      </p>
    </header>
    <main role="main">
    <%= yield %>
    </main>
    <footer>
      <%= partial "address" %>
      <%= partial "hostOn" %>
    </footer>
  </body>
</html>
