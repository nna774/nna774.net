language: ruby
rvm:
  - 2.3.3
install:
  - time bundle install --without production --deployment --jobs=4
script:
  - time bundle exec rake
notifications:
  email:
    on_success: never
sudo: false
cache:
  apt: true
  directories:
  - vendor/bundle
